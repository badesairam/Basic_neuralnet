# Basic Neural net with back tracking algorithm

Implemented batch update algorithm with 1 hidden layer.

The whole data is taken as an matrix and all the Weight updates are implemented as matrix update.

